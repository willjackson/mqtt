# MQTT events.
mqtt_subscription_check:
  label: 'MQTT Subscriptions are retrieved'
  category: 'MQTT'
  context_definitions:
    subscription:
      type: 'entity:mqtt_subscription'
      label: 'Subscription'
